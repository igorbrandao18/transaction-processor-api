name: Deploy to Production

on:
  push:
    branches:
      - main

env:
  SSH_HOST: ${{ secrets.SSH_HOST }}
  SSH_USER: ${{ secrets.SSH_USER }}
  SSH_PASSWORD: ${{ secrets.SSH_PASSWORD }}
  DB_NAME: ${{ secrets.DB_NAME }}
  DB_USER: ${{ secrets.DB_USER }}
  DB_PASSWORD: ${{ secrets.DB_PASSWORD }}
  DOMAIN: ${{ secrets.DOMAIN }}

jobs:
  lint:
    name: Lint Check
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run lint
        run: npm run lint:check

  test-unit:
    name: Unit Tests
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run unit tests
        env:
          NODE_ENV: test
        run: npm run test:unit

  setup:
    name: 🔌 Setup and Verify SSH Connection
    runs-on: ubuntu-latest
    needs: test-unit
    
    steps:
      - name: 💾 Cache apt packages
        uses: actions/cache@v4
        with:
          path: /var/cache/apt
          key: apt-${{ runner.os }}-sshpass
          restore-keys: |
            apt-${{ runner.os }}-

      - name: 📥 Install SSH tools
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "📥 Installing SSH tools (sshpass)..."
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          sudo apt-get update -qq
          sudo apt-get install -y --no-install-recommends sshpass
          echo "✅ sshpass installed successfully"
          sshpass -V || true
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

      - name: 🔍 Test SSH connection
        run: |
          echo "::add-mask::$SSH_PASSWORD"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "🔍 Testing SSH connection to deployment server..."
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "📋 Connection details:"
          echo "   Host: $SSH_HOST"
          echo "   User: $SSH_USER"
          echo "   Method: Password authentication"
          echo ""
          echo "🔨 Attempting SSH connection..."
          if sshpass -p "$SSH_PASSWORD" ssh \
            -o StrictHostKeyChecking=no \
            -o UserKnownHostsFile=/dev/null \
            -o ConnectTimeout=10 \
            $SSH_USER@$SSH_HOST "echo '✅ SSH connection successful' && hostname && uname -a"; then
            echo ""
            echo "✅ SUCCESS: SSH connection established successfully!"
          else
            echo ""
            echo "❌ ERROR: Failed to establish SSH connection"
            exit 1
          fi
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

  backup:
    name: 💾 Backup Database
    runs-on: ubuntu-latest
    needs: setup
    
    steps:
      - name: 💾 Cache apt packages
        uses: actions/cache@v4
        with:
          path: /var/cache/apt
          key: apt-${{ runner.os }}-sshpass
          restore-keys: |
            apt-${{ runner.os }}-

      - name: 📥 Install SSH tools
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "📥 Installing SSH tools (sshpass)..."
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          sudo apt-get update -qq
          sudo apt-get install -y --no-install-recommends sshpass
          echo "✅ sshpass installed"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

      - name: 💾 Create and cleanup database backup
        run: |
          echo "::add-mask::$SSH_PASSWORD"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "💾 Creating database backup and cleaning up old backups..."
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          sshpass -p "$SSH_PASSWORD" ssh \
            -o StrictHostKeyChecking=no \
            -o UserKnownHostsFile=/dev/null \
            $SSH_USER@$SSH_HOST "DB_NAME='$DB_NAME' DB_USER='$DB_USER' bash -s" << 'ENDSSH'
            echo "🔍 Checking PostgreSQL container..."
            if docker ps --format '{{.Names}}' | grep -q 'transaction-db'; then
              echo "✅ PostgreSQL container is running"
              
              BACKUP_DIR="/tmp/backups"
              mkdir -p "$BACKUP_DIR"
              TIMESTAMP=$(date +%Y%m%d_%H%M%S)
              BACKUP_FILE="$BACKUP_DIR/${DB_NAME}_${TIMESTAMP}.sql"
              
              echo "💾 Creating backup: $BACKUP_FILE"
              docker exec transaction-db pg_dump -U "$DB_USER" -d "$DB_NAME" > "$BACKUP_FILE" 2>&1 || echo "⚠️ Backup may have failed (OK for first deployment)"
              
              if [ -f "$BACKUP_FILE" ] && [ -s "$BACKUP_FILE" ]; then
                BACKUP_SIZE=$(du -h "$BACKUP_FILE" | cut -f1)
                echo "✅ Backup created: $BACKUP_SIZE"
              fi
              
              echo "🧹 Cleaning up old backups (keeping last 5)..."
              BACKUP_COUNT=$(ls -1 "$BACKUP_DIR/${DB_NAME}"_*.sql 2>/dev/null | wc -l)
              if [ "$BACKUP_COUNT" -gt 5 ]; then
                cd "$BACKUP_DIR"
                ls -t "${DB_NAME}"_*.sql 2>/dev/null | tail -n +6 | xargs rm -f || true
                echo "✅ Old backups removed"
              fi
            else
              echo "ℹ️ PostgreSQL container not running (OK for first deployment)"
            fi
            echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          ENDSSH

  prepare-deployment:
    name: 📦 Prepare Deployment
    runs-on: ubuntu-latest
    needs: backup
    
    steps:
      - name: 💾 Cache apt packages
        uses: actions/cache@v4
        with:
          path: /var/cache/apt
          key: apt-${{ runner.os }}-sshpass
          restore-keys: |
            apt-${{ runner.os }}-

      - name: 📥 Install SSH tools
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "📥 Installing SSH tools (sshpass)..."
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          sudo apt-get update -qq
          sudo apt-get install -y --no-install-recommends sshpass
          echo "✅ sshpass installed"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

      - name: 📥 Clone repository and prepare archive
        env:
          REPO_URL: https://github.com/${{ github.repository }}.git
        run: |
          echo "::add-mask::$SSH_PASSWORD"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "📦 Preparing deployment (clone + archive)..."
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          sshpass -p "$SSH_PASSWORD" ssh \
            -o StrictHostKeyChecking=no \
            -o UserKnownHostsFile=/dev/null \
            $SSH_USER@$SSH_HOST << ENDSSH
            echo "📥 Cloning repository..."
            mkdir -p /tmp
            cd /tmp
            rm -rf transaction-processor-api
            git clone $REPO_URL transaction-processor-api
            cd transaction-processor-api
            
            echo "📦 Creating application archive..."
            tar -czf app.tar.gz \
              --exclude='node_modules' --exclude='dist' --exclude='build' \
              --exclude='test' --exclude='coverage' --exclude='*.spec.ts' \
              --exclude='.git' --exclude='docker' --exclude='*.md' \
              src/ package*.json tsconfig*.json nest-cli.json migrations/ \
              eslint.config.mjs .prettierrc 2>&1
            
            echo "✅ Archive created: $(du -h app.tar.gz | cut -f1)"
            echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          ENDSSH

  build-and-deploy:
    name: 🚀 Build and Deploy
    runs-on: ubuntu-latest
    needs: prepare-deployment
    
    steps:
      - name: 💾 Cache apt packages
        uses: actions/cache@v4
        with:
          path: /var/cache/apt
          key: apt-${{ runner.os }}-sshpass
          restore-keys: |
            apt-${{ runner.os }}-

      - name: 📥 Install SSH tools
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "📥 Installing SSH tools (sshpass)..."
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          sudo apt-get update -qq
          sudo apt-get install -y --no-install-recommends sshpass
          echo "✅ sshpass installed"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

      - name: 🚀 Stop, build and start containers
        run: |
          echo "::add-mask::$SSH_PASSWORD"
          echo "::add-mask::$DB_PASSWORD"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "🚀 Building and deploying containers..."
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          sshpass -p "$SSH_PASSWORD" ssh \
            -o StrictHostKeyChecking=no \
            -o UserKnownHostsFile=/dev/null \
            $SSH_USER@$SSH_HOST "DB_NAME='$DB_NAME' DB_USER='$DB_USER' DB_PASSWORD='$DB_PASSWORD' bash -s" << 'ENDSSH'
            cd /tmp/transaction-processor-api/docker
            
            echo "🛑 Stopping existing API containers only..."
            echo "⚠️  IMPORTANT: This will only affect API containers defined in docker-compose.yml"
            echo "⚠️  Containers: transaction-api, transaction-db, transaction-redis, transaction-pgadmin"
            echo "⚠️  Frontend and other containers will remain untouched"
            echo "⚠️  Database volumes will be preserved (no data loss)"
            
            # Stop only containers defined in this docker-compose.yml (API containers)
            # docker compose down only affects containers in the current docker-compose.yml file
            # IMPORTANT: Do NOT use -v flag to preserve database volumes
            docker compose down || true
            
            echo "✅ Cleanup completed - only API containers from docker-compose.yml were affected"
            echo "✅ Database volumes preserved (production data safe)"
            
            echo "🔨 Building containers with cache optimization..."
            export DOCKER_BUILDKIT=1
            export COMPOSE_DOCKER_CLI_BUILD=1
            echo "📥 Pulling base images (using cache)..."
            docker compose pull || true
            echo "🔨 Building containers (reusing cached layers)..."
            docker compose build --pull --progress=plain
            
            echo "🚀 Starting containers..."
            docker compose up -d
            
            echo "⏳ Waiting for services to initialize..."
            sleep 15
            
            cd /tmp/transaction-processor-api
            rm -f app.tar.gz || true
            
            echo "📊 Container status:"
            docker compose ps
            echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          ENDSSH

  wait-services:
    name: ⏳ Wait Services and Restore Backup
    runs-on: ubuntu-latest
    needs: build-and-deploy
    
    steps:
      - name: 💾 Cache apt packages
        uses: actions/cache@v4
        with:
          path: /var/cache/apt
          key: apt-${{ runner.os }}-sshpass
          restore-keys: |
            apt-${{ runner.os }}-

      - name: 📥 Install SSH tools
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "📥 Installing SSH tools (sshpass)..."
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          sudo apt-get update -qq
          sudo apt-get install -y --no-install-recommends sshpass
          echo "✅ sshpass installed"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

      - name: ⏳ Wait for services and restore backup
        run: |
          echo "::add-mask::$SSH_PASSWORD"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "⏳ Waiting for services and restoring backup..."
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          sshpass -p "$SSH_PASSWORD" ssh \
            -o StrictHostKeyChecking=no \
            -o UserKnownHostsFile=/dev/null \
            $SSH_USER@$SSH_HOST "DB_NAME='$DB_NAME' DB_USER='$DB_USER' bash -s" << 'ENDSSH'
            echo "⏳ Waiting for PostgreSQL..."
            timeout=60
            elapsed=0
            while [ $elapsed -lt $timeout ]; do
              if docker exec transaction-db pg_isready -U "$DB_USER" > /dev/null 2>&1; then
                echo "✅ PostgreSQL is ready"
                break
              fi
              sleep 2
              elapsed=$((elapsed + 2))
            done
            
            echo "🔍 Checking if production database already exists..."
            DB_EXISTS=$(docker exec transaction-db psql -U "$DB_USER" -lqt 2>/dev/null | cut -d \| -f 1 | grep -qw "$DB_NAME" && echo "yes" || echo "no")
            
            if [ "$DB_EXISTS" = "yes" ]; then
              echo "✅ Production database '$DB_NAME' already exists - preserving existing data"
              echo "📊 Database will use existing data from volume (no data loss)"
              echo "💾 Backup is available for manual rollback if needed"
            else
              echo "📥 Production database '$DB_NAME' does not exist - creating new database..."
              echo "⚠️  This is likely the first deployment"
              docker exec -i transaction-db psql -U "$DB_USER" -d postgres -c "CREATE DATABASE $DB_NAME;" || echo "⚠️ Database may already exist"
              echo "✅ Production database created"
              echo "💾 Backup will be created after migrations for future rollback"
            fi
            
            echo "⏳ Waiting for application..."
            elapsed=0
            while [ $elapsed -lt $timeout ]; do
              if docker ps --format '{{.Names}}' | grep -q 'transaction-api' && \
                 docker exec transaction-api node -e "console.log('OK')" > /dev/null 2>&1; then
                echo "✅ Application is ready"
                break
              fi
              sleep 2
              elapsed=$((elapsed + 2))
            done
            echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          ENDSSH

  finalize-deployment:
    name: 🔄 Finalize Deployment
    runs-on: ubuntu-latest
    needs: wait-services
    
    steps:
      - name: 💾 Cache apt packages
        uses: actions/cache@v4
        with:
          path: /var/cache/apt
          key: apt-${{ runner.os }}-sshpass
          restore-keys: |
            apt-${{ runner.os }}-

      - name: 📥 Install SSH tools
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "📥 Installing SSH tools (sshpass)..."
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          sudo apt-get update -qq
          sudo apt-get install -y --no-install-recommends sshpass
          echo "✅ sshpass installed"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

      - name: 🔄 Run migrations and verify status
        run: |
          echo "::add-mask::$SSH_PASSWORD"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "🔄 Finalizing deployment (migrations + verification)..."
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          sshpass -p "$SSH_PASSWORD" ssh \
            -o StrictHostKeyChecking=no \
            -o UserKnownHostsFile=/dev/null \
            $SSH_USER@$SSH_HOST << 'ENDSSH'
            echo "🔄 Running database migrations..."
            docker exec transaction-api npm run migrate || echo "⚠️ Migrations may have already been applied"
            
            echo "📊 Deployment status:"
            cd /tmp/transaction-processor-api/docker
            docker compose ps
            echo ""
            echo "📋 Application logs (last 20 lines):"
            docker compose logs --tail=20 transaction-api || true
            echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          ENDSSH

  verify:
    name: ✅ Verify Deployment
    runs-on: ubuntu-latest
    needs: finalize-deployment
    
    steps:
      - name: 🔍 Verify HTTP redirects to HTTPS
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "🔍 Verifying HTTP redirects to HTTPS..."
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "📋 Test configuration:"
          echo "   Domain: $DOMAIN"
          echo "   Endpoint: /health"
          echo "   Expected: HTTP redirect (301 or 302) to HTTPS"
          echo ""
          echo "🔨 Testing HTTP endpoint..."
          HTTP_STATUS=$(curl -s -o /dev/null -w "%{http_code}" http://$DOMAIN/health || echo "000")
          echo "   HTTP Status Code: $HTTP_STATUS"
          echo ""
          
          if [ "$HTTP_STATUS" = "301" ] || [ "$HTTP_STATUS" = "302" ]; then
            echo "✅ SUCCESS: HTTP correctly redirects to HTTPS"
          else
            echo "⚠️  WARNING: HTTP returned status $HTTP_STATUS (expected 301 or 302)"
            echo "   This may indicate Nginx is not configured for HTTPS redirect"
          fi
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

      - name: 🔒 Verify HTTPS endpoint
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "🔒 Verifying HTTPS endpoint..."
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "📋 Test configuration:"
          echo "   Domain: $DOMAIN"
          echo "   Endpoint: /health"
          echo "   Protocol: HTTPS"
          echo "   Expected: HTTP 200 OK"
          echo ""
          echo "🔨 Testing HTTPS endpoint..."
          HTTPS_STATUS=$(curl -s -o /dev/null -w "%{http_code}" https://$DOMAIN/health || echo "000")
          RESPONSE_TIME=$(curl -s -o /dev/null -w "%{time_total}" https://$DOMAIN/health || echo "0")
          echo "   HTTPS Status Code: $HTTPS_STATUS"
          echo "   Response Time: ${RESPONSE_TIME}s"
          echo ""
          
          if [ "$HTTPS_STATUS" = "200" ]; then
            echo "✅ SUCCESS: Deployment successful! HTTPS is working."
          else
            echo "❌ ERROR: HTTPS endpoint returned status $HTTPS_STATUS (expected 200)"
            echo "   Deployment verification failed"
            exit 1
          fi
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

      - name: ✅ Verify health endpoint response
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "✅ Verifying health endpoint response..."
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "📋 Test configuration:"
          echo "   Endpoint: https://$DOMAIN/health"
          echo "   Expected: JSON response with status: UP"
          echo ""
          echo "🔨 Fetching health endpoint response..."
          RESPONSE=$(curl -s https://$DOMAIN/health)
          echo "   Response: $RESPONSE"
          echo ""
          
          if echo "$RESPONSE" | grep -q '"status":"UP"'; then
            echo "✅ SUCCESS: Health endpoint is responding correctly"
            echo "   Application status: UP"
          else
            echo "⚠️  WARNING: Health endpoint response may be incorrect"
            echo "   Response does not contain expected status: UP"
          fi
          echo ""
          echo "📊 Parsed health check details:"
          echo "$RESPONSE" | grep -o '"status":"[^"]*"' || echo "   Unable to parse status"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "🎉 Deployment verification completed successfully!"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
